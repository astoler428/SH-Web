import React from 'react'

export default function Action() {
  return (
    <div>

    </div>
  )
}

/**
 * section on bottom with option to take an action or instructions like vote or choose a chan
 * or if not turn - provide update on status - waiting for x to choose a chancellor
 */